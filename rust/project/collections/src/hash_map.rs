fn main() {
    use std::collections::HashMap;
    // HashMap是最不常用的，因此需要自己导入
    let mut scores = HashMap::new();
    scores.insert(String::from("Blue"), 10);
    scores.insert(String::from("Yello"), 50);


}